export enum ActionType {
	Pure = "",
	Async = "_ASYNC",
	Fulfilled = "_FULFILLED",
	Failed = "_FAILED",
}

export enum SortingMethod {
	LatestFirst = "latestFirst",
	LastFirst = "lastFirst",
	Random = "random",
}
